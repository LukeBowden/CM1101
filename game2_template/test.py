import string
user_input = "How about I go through that little passage to the south..."
#no_punct = remove_punct(user_input).lower()
#word_list = str.split(user_input)
    #no_punct = []
    #for word in word_list:
        #if not (word in skip_words):
            #no_punct = no_punct + word
skip_words = ["weew", "kkr"]
"""words = ["weew", "word"]
word_list = []
    for x in words:
        check = True
        for y in skip_words:
            if x == y:
                check = False
        if check == True:
            word_list = word_list.append(x)        
print(word_list)"""
word_list = ["y","x"]
word_list.append("h")
print(len(word_list))
input()